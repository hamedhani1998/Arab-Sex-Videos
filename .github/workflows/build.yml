name: Build and Publish

concurrency:
  group: "build"
  cancel-in-progress: true

on:
  push:
    branches:
      - main
    paths-ignore:
      - '*.md'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          path: "src"

      - name: Checkout builds
        uses: actions/checkout@v4
        with:
          ref: "builds"
          path: "builds"

      - name: Clean old builds
        run: rm -f $GITHUB_WORKSPACE/builds/*.cs3 $GITHUB_WORKSPACE/builds/plugins.json

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Make gradlew executable
        run: chmod +x src/gradlew

      - name: Build Plugins
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          cd $GITHUB_WORKSPACE/src
          ./gradlew make makePluginsJson
          cp **/build/*.cs3 $GITHUB_WORKSPACE/builds
          cp build/plugins.json $GITHUB_WORKSPACE/builds

      - name: Arabic labels in plugins.json
        run: |
          python3 - <<'EOF'
          import json
          label_map = {
            "SexAlarab": "سكس العرب",
            "SexAlarabNet": "سكس العرب نت",
            "ArabXCam": "عرب اكس",
            "XHorno": "xhorno",
            "ArabXN": "عرب xn",
            "ArabXNSex": "arabxn.sex",
          }
          p = "$GITHUB_WORKSPACE/builds/plugins.json"
          with open(p, encoding="utf-8") as fh:
              plugins = json.load(fh)
          for pl in plugins:
              internal = pl.get("internalName", "")
              if internal in label_map:
                  pl["name"] = label_map[internal]
              pl["repositoryUrl"] = "https://github.com/${{ github.repository }}"
              pl.setdefault("language", "ar")
          with open(p, "w", encoding="utf-8") as fh:
              json.dump(plugins, fh, ensure_ascii=False, indent=2)
          print("plugins:", [x.get("name") for x in plugins])
          EOF

      - name: Push builds
        run: |
          cd $GITHUB_WORKSPACE/builds
          git config --local user.email "hamedhani1998@users.noreply.github.com"
          git config --local user.name "hamedhani1998"
          git add .
          if git diff --cached --quiet; then
            echo "Nothing to commit"
          else
            git commit --amend -m "Build $GITHUB_SHA"
            git push --force
          fi